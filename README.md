# NetworkLayer_Analyzer_Packet_Loss_Estimator_Mode-
An Agentic AI framework that scans the Network layer of a host device and analyzes the network to estimate packet loss and informs the host device at the earliest
